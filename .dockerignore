node_modules/
npm-debug.log*
.env
.env.example
.git/
.github/
.gitignore
*.md
LICENSE
# NOTE: cache/ is intentionally NOT ignored — Dockerfile COPYs it so the
# named volume gets seeded with skillData.json + icons on first start.
